interface Flex {
  flex: number
}

interface Margin {
  margin: number
}

interface MarginLeft {
  marginLeft: number
}

interface MarginRight {
  marginRight: number
}

interface MarginTop {
  marginTop: number
}

interface MarginBottom {
  marginBottom: number
}

interface MarginVertical {
  marginVertical: number
}

interface MarginHorizontal {
  marginHorizontal: number
}

interface Padding {
  padding: number
}

interface PaddingLeft {
  paddingLeft: number
}

interface PaddingRight {
  paddingRight: number
}

interface PaddingTop {
  paddingTop: number
}

interface PaddingBottom {
  paddingBottom: number
}

interface PaddingVertical {
  paddingVertical: number
}

interface PaddingHorizontal {
  paddingHorizontal: number
}

interface Width {
  width: number | string
}
interface Height {
  height: number | string
}
interface LineHeight {
  lineHeight: number | string
}

export type {
  Flex,
  Margin,
  MarginLeft,
  MarginRight,
  MarginTop,
  MarginBottom,
  MarginVertical,
  MarginHorizontal,
  Padding,
  PaddingLeft,
  PaddingRight,
  PaddingTop,
  PaddingBottom,
  PaddingVertical,
  PaddingHorizontal,
  Width,
  Height,
  LineHeight,
};
